rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/images/{allImages=**} {
      allow read, write: if request.auth != null; // Allows authenticated users to read and write
    }
    
    // If you want to restrict access further, you can specify additional rules here
  }
}
